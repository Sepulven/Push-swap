how to install the visualizer


1. C++17
2. SFML
3. CMAKE

4. These packages:
	sudo apt-get install libx11-dev libxrandr-dev -> for x11 
	sudo apt-get install libudev-dev -> for udev
	sudo apt-get install libfreetype6-dev -> freetype

all of these packages are needed for the installation

